import styles from './NotFoundPage.module.css';

const NotFoundPage = () => {
  return (
    <div className={styles.fullInset}>
      <span className={styles.text}>404</span>
    </div>
  );
};

export default NotFoundPage;
