# Confchair

## Get started

Set the backend IP (VITE_API_BASE_URL) in `.env` file near the `.package.json`:

```
VITE_API_BASE_URL=...
```

Download dependencies:

```
npm i
```

Start project

```
npm run dev
```
